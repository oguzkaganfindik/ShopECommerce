@model List<ResultShopTableDto>

@{
    ViewData["Title"] = "Index";
    int count = 0;
}

<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">ShopTable İşlemleri</h4>
        <div class="row">
            <div class="col-md-12">

                <div class="card">
                    <div class="card-header">
                        <div class="card-title">ShopTable Listesi</div>
                    </div>
                    <div class="card-body">
                        <div class="card-sub">
                            ShopTable ile ilgili işlemleri aşağıdan gerçekleştirebilirsiniz.
                        </div>
                        <table class="table mt-3">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">ShopTable Adı</th>
                                    <th scope="col">Sil</th>
                                    <th scope="col">Güncelle</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr>
                                        <th>@count</th>
                                        <td>@item.Name</td>
                                        <td><a href="/Admin/ShopTable/DeleteShopTable/@item.Id" class="btn btn-outline-danger btn-round">Sil</a></td>
                                        <td><a href="/Admin/ShopTable/UpdateShopTable/@item.Id" class="btn btn-outline-success btn-round">Güncelle</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/Admin/ShopTable/CreateShopTable/" class="btn btn-outline-primary btn-round">Yeni ShopTable Girişi</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

