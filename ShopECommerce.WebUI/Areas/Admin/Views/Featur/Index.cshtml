@model List<ResultFeaturViewModel>

@{
    ViewData["Title"] = "Index";
}

<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Ana Sayfa Featur İşlemleri</h4>
        <div class="row">
            <div class="col-md-12">

                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Featur Listesi</div>
                    </div>
                    <div class="card-body">
                        <div class="card-sub">
                            Featur alanıyla ile ilgili işlemleri yapabilirsiniz. Tema bütünlüğü için sadece 4 tane Featur alanı oluşturmayı unutmayın.
                        </div>
                        <table class="table mt-3">
                            <thead>
                                <tr>
                                    <th scope="col">Başlık</th>
                                    <th scope="col">Açıklama</th>
                                    <th scope="col">Icon</th>
                                    <th scope="col">Durum</th>
                                    <th scope="col">Sil</th>
                                    <th scope="col">Güncelle</th>
                                    <th scope="col">Durum</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Title</td>
                                        <td>@item.Description</td>
                                        <td>@item.Icon</td>
                                        <td>@item.Status</td>
                                        <td><a href="/Admin/Featur/DeleteFeatur/@item.Id" class="btn btn-outline-danger btn-round">Sil</a></td>
                                        <td><a href="/Admin/Featur/UpdateFeatur/@item.Id" class="btn btn-outline-success btn-round">Güncelle</a></td>
                                        <td><a href="/Admin/Featur/ToggleStatus/@item.Id" class="btn btn-outline-dark btn-round">Durumu Değiştir</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/Admin/Featur/CreateFeatur/" class="btn btn-outline-primary btn-round">Yeni Featur Yazısı Girişi</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

